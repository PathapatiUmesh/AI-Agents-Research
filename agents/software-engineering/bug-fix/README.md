# Bug-Fix Agent (planned)

Agent that triages issues in large codebases: reproduce, locate root cause, propose patch, run tests.

**Planned stack:** AutoGen or LangGraph + code execution sandbox.

**Prerequisites:** Job-search agent Phase 1 complete; local repo indexing (ripgrep / tree-sitter).
