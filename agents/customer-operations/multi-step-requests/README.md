# Multi-Step Request Agent (planned)

Handles multi-step user requests (e.g. "reset access, notify user, update CMDB") with checkpointed state.

**Planned stack:** LangGraph with interrupt/resume for approvals.
